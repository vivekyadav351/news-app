import Styled from 'styled-components';

export const LayoutStyle = Styled.div `

    display: flex;
    flex-direction: column;
    min-height: 100%;

`;

export const MainStyle = Styled.main `

    min-height: 80vh;
    margin: 12vh 10px 0 10px;

`;

